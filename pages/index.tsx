import { Button } from '@mui/material';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div>
      <Button variant="contained">Hello</Button>
    </div>
  );
};

export default Home;
